.page-header.header-filter.clear-filter.purple-filter
  .container
    -# logo
    .row
      .col-md-8.ml-auto.mr-auto
        %h2.title.text-left Connecting great jobs to great candidates
    .row
      .col-md-8.ml-auto.mr-auto
        .mt-5{:style => "width: 100%"}
          = form_with(url: pages_jobs_path, method: 'get') do |f|
            .form-row.my-search{style: "justify-content: center;align-items: baseline;"}
              .form-group.bmd-form-group.col-md-4.py-2.pr-2
                -# = f.label :key_search, 'Search for a key search '
                = f.search_field :key_search, placeholder: 'What job title, kewords, or company', class: 'form-control text-white'
              .form-group.col-md-4.py-2.pr-2
                -# = f.label :location, 'Search for a location '
                = f.search_field :location, placeholder: 'Where London, UK, Post code', class: 'form-control text-white'
              -# .form-group.col-md-4.py-2.px-0
              -#   = label :skills, 'Search for a skills '
              -#   = search_field :skills, nil, placeholder: 'Eg. full stack', class: 'form-control'
              -# .form-group.col-md-4.py-2.px-0
              -#   = label :job_type, 'Search for a job type '
              -#   = search_field :job_type, nil, placeholder: 'Eg. full', class: 'form-control'
              .form-group.col-md-3.py-2.px-0
                = f.button 'search', class: 'btn btn-primary btn-block btn-round' do
                  %i.material-icons search
                  Search job

.main.main-raised
  .no-child-div.pl-5.pr-5.margin-top-minus
    .navbar.navbar-expand-lg.navbar-light.bg-primary
      .container
        %ul.categories-list
          %li 1
          %li 2

  .container
    .row
      .col-lg-12.mt-5.mx-auto
        
  .container
    .row
      .col-lg-12.mt-5


        - if @info
          - @info.each do |job|
            .card
              .card-body
                %h3.title.card-category.text-primary
                  %i.material-icons work
                  = job['title']
                %h4.card-title
                  %a(href="#{job['url']}")
                    = job['company']
                %h5= job['location']
                %p= job['description'].truncate(500).html_safe

                .card-stats
                  .author
                    = job['created_at']
                    = link_to job['title'], show_job_path(job['id'])
                  .stats.ml-auto
                    .material-icons
                      %a{href: "", title: "Save job"}favorite
                    -# .material-icons share
          -# .card.text-left
          -#   -# - if job['company_logo']
          -#     -# .card-header-image.pb-5{style:"overflow: hidden;"}

          -#       -# %img{alt:"company_logo", style:"max-width:5rem;"}(src="#{job['company_logo']}")/
          -#   .card-header.card-header-info
          -#     %ul.nav.nav-tabs
          -#       %li.nav-item
          -#         %a.nav-link.active(href="#0") Active
          -#       %li.nav-item
          -#         %a.nav-link(href="#0") link
          -#   .card-body
          -#     %h3
          -#       Type
          -#       = job['type']
          -#     %a{href: "#{job['company_url']}"}= job['company']
          -#     %h3
          -#       Location:
          -#       = job['location']
          -#     %h5.card-category.card-category-social
          -#       %i.material-icons work
          -#     %h4.card-title

          -#     .card-text
          -#       = (job['description'].truncate(500)).html_safe
          -#     %a.btn.btn-primary(href="#{job['url']}") Web
          -#     .card-stats

          -#       .author
          -#       .stats.ml-auto
          -#         %i.material-icons today
          -#         =raw job['created_at']
  -# %h1 Response
  -# - if params[:key_search]
  -#   %h6.text-bold
  -#     key search
  -#     = params[:key_search]
  -# - if params[:localization]
  -#   %h6.text-bold
  -#     Localization
  -#     = params[:localization]
  -# - if params[:skills]
  -#   %h6.text-bold
  -#     Skils
  -#     = params[:skills]
  -# - if params[:job_type]
  -#   %h6.text-bold
  -#     Jop Type
  -#     = params[:job_type]
